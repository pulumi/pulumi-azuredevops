// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Use this data source to query all available service endpoint types in Azure DevOps.
 *
 * > **NOTE:** If you need to find a specific service endpoint type by name, consider using the azuredevops.getServiceendpointType data source.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as azuredevops from "@pulumi/azuredevops";
 *
 * const all = azuredevops.getServiceendpointTypes({});
 * export const allTypes = all.then(all => all.types);
 * ```
 *
 * ### Filter service endpoint types
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as azuredevops from "@pulumi/azuredevops";
 * import * as std from "@pulumi/std";
 *
 * const all = azuredevops.getServiceendpointTypes({});
 * const gitTypes = all.then(all => .filter(type => std.regexall({
 *     pattern: "git",
 *     string: std.lower({
 *         input: type.name,
 *     }).result,
 * }).result.length > 0).map(type => (type)));
 * export const gitRelatedTypes = gitTypes;
 * ```
 *
 * ## Relevant Links
 *
 * - [Azure DevOps Service REST API 7.0 - Service Endpoint Types](https://learn.microsoft.com/en-us/rest/api/azure/devops/serviceendpoint/types/list)
 */
export function getServiceendpointTypes(opts?: pulumi.InvokeOptions): Promise<GetServiceendpointTypesResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("azuredevops:index/getServiceendpointTypes:getServiceendpointTypes", {
    }, opts);
}

/**
 * A collection of values returned by getServiceendpointTypes.
 */
export interface GetServiceendpointTypesResult {
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * A list of service endpoint types. Each type has the following attributes:
     */
    readonly types: outputs.GetServiceendpointTypesType[];
}
/**
 * Use this data source to query all available service endpoint types in Azure DevOps.
 *
 * > **NOTE:** If you need to find a specific service endpoint type by name, consider using the azuredevops.getServiceendpointType data source.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as azuredevops from "@pulumi/azuredevops";
 *
 * const all = azuredevops.getServiceendpointTypes({});
 * export const allTypes = all.then(all => all.types);
 * ```
 *
 * ### Filter service endpoint types
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as azuredevops from "@pulumi/azuredevops";
 * import * as std from "@pulumi/std";
 *
 * const all = azuredevops.getServiceendpointTypes({});
 * const gitTypes = all.then(all => .filter(type => std.regexall({
 *     pattern: "git",
 *     string: std.lower({
 *         input: type.name,
 *     }).result,
 * }).result.length > 0).map(type => (type)));
 * export const gitRelatedTypes = gitTypes;
 * ```
 *
 * ## Relevant Links
 *
 * - [Azure DevOps Service REST API 7.0 - Service Endpoint Types](https://learn.microsoft.com/en-us/rest/api/azure/devops/serviceendpoint/types/list)
 */
export function getServiceendpointTypesOutput(opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetServiceendpointTypesResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("azuredevops:index/getServiceendpointTypes:getServiceendpointTypes", {
    }, opts);
}
