# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities

__all__ = ['SecurityPermissionsArgs', 'SecurityPermissions']

@pulumi.input_type
class SecurityPermissionsArgs:
    def __init__(__self__, *,
                 namespace_id: pulumi.Input[_builtins.str],
                 permissions: pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]],
                 principal: pulumi.Input[_builtins.str],
                 token: pulumi.Input[_builtins.str],
                 replace: Optional[pulumi.Input[_builtins.bool]] = None):
        """
        The set of arguments for constructing a SecurityPermissions resource.
        :param pulumi.Input[_builtins.str] namespace_id: The ID of the security namespace. Use the `get_security_namespaces` data source to discover available namespaces. Changing this forces a new resource to be created.
        :param pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]] permissions: A map of permission names to permission values. All permission names specified must be valid for the given namespace, or an error will be returned. Permission values must be one of:
               - `Allow` (or `allow`, `ALLOW`) - Grant the permission
               - `Deny` (or `deny`, `DENY`) - Explicitly deny the permission
               - `NotSet` (or `notset`, `NOTSET`) - Remove the permission (inherit from parent)
        :param pulumi.Input[_builtins.str] principal: The descriptor or identity ID of the principal (user or group). Changing this forces a new resource to be created.
        :param pulumi.Input[_builtins.str] token: The security token for the resource. Use the `get_security_namespace_token` data source to generate tokens for specific resources. Changing this forces a new resource to be created.
        :param pulumi.Input[_builtins.bool] replace: Replace (`true`) or merge (`false`) the permissions with existing permissions. When `true`, all existing permissions for the principal on this token will be replaced with the specified permissions. When `false`, the specified permissions will be merged with existing permissions. Default: `true`.
        """
        pulumi.set(__self__, "namespace_id", namespace_id)
        pulumi.set(__self__, "permissions", permissions)
        pulumi.set(__self__, "principal", principal)
        pulumi.set(__self__, "token", token)
        if replace is not None:
            pulumi.set(__self__, "replace", replace)

    @_builtins.property
    @pulumi.getter(name="namespaceId")
    def namespace_id(self) -> pulumi.Input[_builtins.str]:
        """
        The ID of the security namespace. Use the `get_security_namespaces` data source to discover available namespaces. Changing this forces a new resource to be created.
        """
        return pulumi.get(self, "namespace_id")

    @namespace_id.setter
    def namespace_id(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "namespace_id", value)

    @_builtins.property
    @pulumi.getter
    def permissions(self) -> pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]]:
        """
        A map of permission names to permission values. All permission names specified must be valid for the given namespace, or an error will be returned. Permission values must be one of:
        - `Allow` (or `allow`, `ALLOW`) - Grant the permission
        - `Deny` (or `deny`, `DENY`) - Explicitly deny the permission
        - `NotSet` (or `notset`, `NOTSET`) - Remove the permission (inherit from parent)
        """
        return pulumi.get(self, "permissions")

    @permissions.setter
    def permissions(self, value: pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]]):
        pulumi.set(self, "permissions", value)

    @_builtins.property
    @pulumi.getter
    def principal(self) -> pulumi.Input[_builtins.str]:
        """
        The descriptor or identity ID of the principal (user or group). Changing this forces a new resource to be created.
        """
        return pulumi.get(self, "principal")

    @principal.setter
    def principal(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "principal", value)

    @_builtins.property
    @pulumi.getter
    def token(self) -> pulumi.Input[_builtins.str]:
        """
        The security token for the resource. Use the `get_security_namespace_token` data source to generate tokens for specific resources. Changing this forces a new resource to be created.
        """
        return pulumi.get(self, "token")

    @token.setter
    def token(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "token", value)

    @_builtins.property
    @pulumi.getter
    def replace(self) -> Optional[pulumi.Input[_builtins.bool]]:
        """
        Replace (`true`) or merge (`false`) the permissions with existing permissions. When `true`, all existing permissions for the principal on this token will be replaced with the specified permissions. When `false`, the specified permissions will be merged with existing permissions. Default: `true`.
        """
        return pulumi.get(self, "replace")

    @replace.setter
    def replace(self, value: Optional[pulumi.Input[_builtins.bool]]):
        pulumi.set(self, "replace", value)


@pulumi.input_type
class _SecurityPermissionsState:
    def __init__(__self__, *,
                 namespace_id: Optional[pulumi.Input[_builtins.str]] = None,
                 permissions: Optional[pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]]] = None,
                 principal: Optional[pulumi.Input[_builtins.str]] = None,
                 replace: Optional[pulumi.Input[_builtins.bool]] = None,
                 token: Optional[pulumi.Input[_builtins.str]] = None):
        """
        Input properties used for looking up and filtering SecurityPermissions resources.
        :param pulumi.Input[_builtins.str] namespace_id: The ID of the security namespace. Use the `get_security_namespaces` data source to discover available namespaces. Changing this forces a new resource to be created.
        :param pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]] permissions: A map of permission names to permission values. All permission names specified must be valid for the given namespace, or an error will be returned. Permission values must be one of:
               - `Allow` (or `allow`, `ALLOW`) - Grant the permission
               - `Deny` (or `deny`, `DENY`) - Explicitly deny the permission
               - `NotSet` (or `notset`, `NOTSET`) - Remove the permission (inherit from parent)
        :param pulumi.Input[_builtins.str] principal: The descriptor or identity ID of the principal (user or group). Changing this forces a new resource to be created.
        :param pulumi.Input[_builtins.bool] replace: Replace (`true`) or merge (`false`) the permissions with existing permissions. When `true`, all existing permissions for the principal on this token will be replaced with the specified permissions. When `false`, the specified permissions will be merged with existing permissions. Default: `true`.
        :param pulumi.Input[_builtins.str] token: The security token for the resource. Use the `get_security_namespace_token` data source to generate tokens for specific resources. Changing this forces a new resource to be created.
        """
        if namespace_id is not None:
            pulumi.set(__self__, "namespace_id", namespace_id)
        if permissions is not None:
            pulumi.set(__self__, "permissions", permissions)
        if principal is not None:
            pulumi.set(__self__, "principal", principal)
        if replace is not None:
            pulumi.set(__self__, "replace", replace)
        if token is not None:
            pulumi.set(__self__, "token", token)

    @_builtins.property
    @pulumi.getter(name="namespaceId")
    def namespace_id(self) -> Optional[pulumi.Input[_builtins.str]]:
        """
        The ID of the security namespace. Use the `get_security_namespaces` data source to discover available namespaces. Changing this forces a new resource to be created.
        """
        return pulumi.get(self, "namespace_id")

    @namespace_id.setter
    def namespace_id(self, value: Optional[pulumi.Input[_builtins.str]]):
        pulumi.set(self, "namespace_id", value)

    @_builtins.property
    @pulumi.getter
    def permissions(self) -> Optional[pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]]]:
        """
        A map of permission names to permission values. All permission names specified must be valid for the given namespace, or an error will be returned. Permission values must be one of:
        - `Allow` (or `allow`, `ALLOW`) - Grant the permission
        - `Deny` (or `deny`, `DENY`) - Explicitly deny the permission
        - `NotSet` (or `notset`, `NOTSET`) - Remove the permission (inherit from parent)
        """
        return pulumi.get(self, "permissions")

    @permissions.setter
    def permissions(self, value: Optional[pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "permissions", value)

    @_builtins.property
    @pulumi.getter
    def principal(self) -> Optional[pulumi.Input[_builtins.str]]:
        """
        The descriptor or identity ID of the principal (user or group). Changing this forces a new resource to be created.
        """
        return pulumi.get(self, "principal")

    @principal.setter
    def principal(self, value: Optional[pulumi.Input[_builtins.str]]):
        pulumi.set(self, "principal", value)

    @_builtins.property
    @pulumi.getter
    def replace(self) -> Optional[pulumi.Input[_builtins.bool]]:
        """
        Replace (`true`) or merge (`false`) the permissions with existing permissions. When `true`, all existing permissions for the principal on this token will be replaced with the specified permissions. When `false`, the specified permissions will be merged with existing permissions. Default: `true`.
        """
        return pulumi.get(self, "replace")

    @replace.setter
    def replace(self, value: Optional[pulumi.Input[_builtins.bool]]):
        pulumi.set(self, "replace", value)

    @_builtins.property
    @pulumi.getter
    def token(self) -> Optional[pulumi.Input[_builtins.str]]:
        """
        The security token for the resource. Use the `get_security_namespace_token` data source to generate tokens for specific resources. Changing this forces a new resource to be created.
        """
        return pulumi.get(self, "token")

    @token.setter
    def token(self, value: Optional[pulumi.Input[_builtins.str]]):
        pulumi.set(self, "token", value)


@pulumi.type_token("azuredevops:index/securityPermissions:SecurityPermissions")
class SecurityPermissions(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 namespace_id: Optional[pulumi.Input[_builtins.str]] = None,
                 permissions: Optional[pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]]] = None,
                 principal: Optional[pulumi.Input[_builtins.str]] = None,
                 replace: Optional[pulumi.Input[_builtins.bool]] = None,
                 token: Optional[pulumi.Input[_builtins.str]] = None,
                 __props__=None):
        """
        Manages permissions for Azure DevOps security namespaces. This is a generic permissions resource that can be used to manage permissions for any security namespace in Azure DevOps.

        > **Note** This is a low-level generic permissions resource. For specific resource types, consider using the dedicated permission resources such as `ProjectPermissions`, `GitPermissions`, `BuildDefinitionPermissions`, etc.

        ## Example Usage

        ### Collection-level Permissions

        ```python
        import pulumi
        import pulumi_azuredevops as azuredevops

        collection = azuredevops.get_security_namespace(name="Collection")
        collection_get_security_namespace_token = azuredevops.get_security_namespace_token(namespace_name="Collection")
        example = azuredevops.get_group(name="Project Collection Administrators")
        collection_perms = azuredevops.SecurityPermissions("collection_perms",
            namespace_id=collection.id,
            token=collection_get_security_namespace_token.token,
            principal=example.descriptor,
            permissions={
                "GENERIC_READ": "allow",
                "GENERIC_WRITE": "allow",
            })
        ```

        ### Git Repository Permissions

        ```python
        import pulumi
        import pulumi_azuredevops as azuredevops

        example = azuredevops.get_project(name="Example Project")
        example_get_git_repository = azuredevops.get_git_repository(project_id=example.id,
            name="Example Repository")
        git_repos = azuredevops.get_security_namespace(name="Git Repositories")
        git_repo = azuredevops.get_security_namespace_token(namespace_name="Git Repositories",
            identifiers={
                "project_id": example.id,
                "repository_id": example_get_git_repository.id,
            })
        example_contributors = azuredevops.get_group(project_id=example.id,
            name="Contributors")
        git_perms = azuredevops.SecurityPermissions("git_perms",
            namespace_id=git_repos.id,
            token=git_repo.token,
            principal=example_contributors.descriptor,
            permissions={
                "GenericRead": "allow",
                "GenericContribute": "allow",
                "ForcePush": "deny",
                "ManagePermissions": "deny",
            },
            replace=False)
        ```

        ### Git Branch Permissions

        ```python
        import pulumi
        import pulumi_azuredevops as azuredevops

        example = azuredevops.get_project(name="Example Project")
        example_get_git_repository = azuredevops.get_git_repository(project_id=example.id,
            name="Example Repository")
        git_repos = azuredevops.get_security_namespace(name="Git Repositories")
        main_branch = azuredevops.get_security_namespace_token(namespace_name="Git Repositories",
            identifiers={
                "project_id": example.id,
                "repository_id": example_get_git_repository.id,
                "ref_name": "refs/heads/main",
            })
        example_contributors = azuredevops.get_group(project_id=example.id,
            name="Contributors")
        main_branch_perms = azuredevops.SecurityPermissions("main_branch_perms",
            namespace_id="2e9eb7ed-3c0a-47d4-87c1-0ffdd275fd87",
            token=main_branch.token,
            principal=example_contributors.descriptor,
            permissions={
                "ForcePush": "Deny",
                "RemoveOthersLocks": "Deny",
            },
            replace=False)
        ```

        ## Relevant Links

        - [Azure DevOps Service REST API 7.0 - Security](https://docs.microsoft.com/en-us/rest/api/azure/devops/security/?view=azure-devops-rest-7.0)
        - [Azure DevOps Security Namespaces](https://docs.microsoft.com/en-us/azure/devops/organizations/security/security-glossary?view=azure-devops)

        ## PAT Permissions Required

        - **Project & Team**: vso.security_manage - Grants the ability to read, write, and manage security permissions.

        ## Notes

        - This is a generic low-level resource for managing permissions across any Azure DevOps security namespace
        - For better user experience and type safety, consider using dedicated permission resources when available (e.g., `ProjectPermissions`, `GitPermissions`)
        - Permission names are namespace-specific and case-sensitive. All permission names in the `permissions` map are validated against the namespace - if any permission name is invalid, an error will be returned
        - When `replace = true`, all existing permissions for the principal will be removed and replaced with the specified permissions
        - When `replace = false`, the specified permissions will be merged with existing permissions, allowing you to manage only a subset of permissions
        - when `replace = false`, deletion of the resource only removes the permissions specified in the `permissions` map, rather than all permissions for the principal
        - The `principal` must be a group descriptor or identity ID. Individual user principals are not supported
        - Use the `get_security_namespace_token` data source to generate correct tokens for different resource types
        - Permissions are propagated asynchronously and may take a few moments to take effect

        ## Import

        The resource does not support import.

        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] namespace_id: The ID of the security namespace. Use the `get_security_namespaces` data source to discover available namespaces. Changing this forces a new resource to be created.
        :param pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]] permissions: A map of permission names to permission values. All permission names specified must be valid for the given namespace, or an error will be returned. Permission values must be one of:
               - `Allow` (or `allow`, `ALLOW`) - Grant the permission
               - `Deny` (or `deny`, `DENY`) - Explicitly deny the permission
               - `NotSet` (or `notset`, `NOTSET`) - Remove the permission (inherit from parent)
        :param pulumi.Input[_builtins.str] principal: The descriptor or identity ID of the principal (user or group). Changing this forces a new resource to be created.
        :param pulumi.Input[_builtins.bool] replace: Replace (`true`) or merge (`false`) the permissions with existing permissions. When `true`, all existing permissions for the principal on this token will be replaced with the specified permissions. When `false`, the specified permissions will be merged with existing permissions. Default: `true`.
        :param pulumi.Input[_builtins.str] token: The security token for the resource. Use the `get_security_namespace_token` data source to generate tokens for specific resources. Changing this forces a new resource to be created.
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: SecurityPermissionsArgs,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        Manages permissions for Azure DevOps security namespaces. This is a generic permissions resource that can be used to manage permissions for any security namespace in Azure DevOps.

        > **Note** This is a low-level generic permissions resource. For specific resource types, consider using the dedicated permission resources such as `ProjectPermissions`, `GitPermissions`, `BuildDefinitionPermissions`, etc.

        ## Example Usage

        ### Collection-level Permissions

        ```python
        import pulumi
        import pulumi_azuredevops as azuredevops

        collection = azuredevops.get_security_namespace(name="Collection")
        collection_get_security_namespace_token = azuredevops.get_security_namespace_token(namespace_name="Collection")
        example = azuredevops.get_group(name="Project Collection Administrators")
        collection_perms = azuredevops.SecurityPermissions("collection_perms",
            namespace_id=collection.id,
            token=collection_get_security_namespace_token.token,
            principal=example.descriptor,
            permissions={
                "GENERIC_READ": "allow",
                "GENERIC_WRITE": "allow",
            })
        ```

        ### Git Repository Permissions

        ```python
        import pulumi
        import pulumi_azuredevops as azuredevops

        example = azuredevops.get_project(name="Example Project")
        example_get_git_repository = azuredevops.get_git_repository(project_id=example.id,
            name="Example Repository")
        git_repos = azuredevops.get_security_namespace(name="Git Repositories")
        git_repo = azuredevops.get_security_namespace_token(namespace_name="Git Repositories",
            identifiers={
                "project_id": example.id,
                "repository_id": example_get_git_repository.id,
            })
        example_contributors = azuredevops.get_group(project_id=example.id,
            name="Contributors")
        git_perms = azuredevops.SecurityPermissions("git_perms",
            namespace_id=git_repos.id,
            token=git_repo.token,
            principal=example_contributors.descriptor,
            permissions={
                "GenericRead": "allow",
                "GenericContribute": "allow",
                "ForcePush": "deny",
                "ManagePermissions": "deny",
            },
            replace=False)
        ```

        ### Git Branch Permissions

        ```python
        import pulumi
        import pulumi_azuredevops as azuredevops

        example = azuredevops.get_project(name="Example Project")
        example_get_git_repository = azuredevops.get_git_repository(project_id=example.id,
            name="Example Repository")
        git_repos = azuredevops.get_security_namespace(name="Git Repositories")
        main_branch = azuredevops.get_security_namespace_token(namespace_name="Git Repositories",
            identifiers={
                "project_id": example.id,
                "repository_id": example_get_git_repository.id,
                "ref_name": "refs/heads/main",
            })
        example_contributors = azuredevops.get_group(project_id=example.id,
            name="Contributors")
        main_branch_perms = azuredevops.SecurityPermissions("main_branch_perms",
            namespace_id="2e9eb7ed-3c0a-47d4-87c1-0ffdd275fd87",
            token=main_branch.token,
            principal=example_contributors.descriptor,
            permissions={
                "ForcePush": "Deny",
                "RemoveOthersLocks": "Deny",
            },
            replace=False)
        ```

        ## Relevant Links

        - [Azure DevOps Service REST API 7.0 - Security](https://docs.microsoft.com/en-us/rest/api/azure/devops/security/?view=azure-devops-rest-7.0)
        - [Azure DevOps Security Namespaces](https://docs.microsoft.com/en-us/azure/devops/organizations/security/security-glossary?view=azure-devops)

        ## PAT Permissions Required

        - **Project & Team**: vso.security_manage - Grants the ability to read, write, and manage security permissions.

        ## Notes

        - This is a generic low-level resource for managing permissions across any Azure DevOps security namespace
        - For better user experience and type safety, consider using dedicated permission resources when available (e.g., `ProjectPermissions`, `GitPermissions`)
        - Permission names are namespace-specific and case-sensitive. All permission names in the `permissions` map are validated against the namespace - if any permission name is invalid, an error will be returned
        - When `replace = true`, all existing permissions for the principal will be removed and replaced with the specified permissions
        - When `replace = false`, the specified permissions will be merged with existing permissions, allowing you to manage only a subset of permissions
        - when `replace = false`, deletion of the resource only removes the permissions specified in the `permissions` map, rather than all permissions for the principal
        - The `principal` must be a group descriptor or identity ID. Individual user principals are not supported
        - Use the `get_security_namespace_token` data source to generate correct tokens for different resource types
        - Permissions are propagated asynchronously and may take a few moments to take effect

        ## Import

        The resource does not support import.

        :param str resource_name: The name of the resource.
        :param SecurityPermissionsArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(SecurityPermissionsArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 namespace_id: Optional[pulumi.Input[_builtins.str]] = None,
                 permissions: Optional[pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]]] = None,
                 principal: Optional[pulumi.Input[_builtins.str]] = None,
                 replace: Optional[pulumi.Input[_builtins.bool]] = None,
                 token: Optional[pulumi.Input[_builtins.str]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = SecurityPermissionsArgs.__new__(SecurityPermissionsArgs)

            if namespace_id is None and not opts.urn:
                raise TypeError("Missing required property 'namespace_id'")
            __props__.__dict__["namespace_id"] = namespace_id
            if permissions is None and not opts.urn:
                raise TypeError("Missing required property 'permissions'")
            __props__.__dict__["permissions"] = permissions
            if principal is None and not opts.urn:
                raise TypeError("Missing required property 'principal'")
            __props__.__dict__["principal"] = principal
            __props__.__dict__["replace"] = replace
            if token is None and not opts.urn:
                raise TypeError("Missing required property 'token'")
            __props__.__dict__["token"] = token
        super(SecurityPermissions, __self__).__init__(
            'azuredevops:index/securityPermissions:SecurityPermissions',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            namespace_id: Optional[pulumi.Input[_builtins.str]] = None,
            permissions: Optional[pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]]] = None,
            principal: Optional[pulumi.Input[_builtins.str]] = None,
            replace: Optional[pulumi.Input[_builtins.bool]] = None,
            token: Optional[pulumi.Input[_builtins.str]] = None) -> 'SecurityPermissions':
        """
        Get an existing SecurityPermissions resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] namespace_id: The ID of the security namespace. Use the `get_security_namespaces` data source to discover available namespaces. Changing this forces a new resource to be created.
        :param pulumi.Input[Mapping[str, pulumi.Input[_builtins.str]]] permissions: A map of permission names to permission values. All permission names specified must be valid for the given namespace, or an error will be returned. Permission values must be one of:
               - `Allow` (or `allow`, `ALLOW`) - Grant the permission
               - `Deny` (or `deny`, `DENY`) - Explicitly deny the permission
               - `NotSet` (or `notset`, `NOTSET`) - Remove the permission (inherit from parent)
        :param pulumi.Input[_builtins.str] principal: The descriptor or identity ID of the principal (user or group). Changing this forces a new resource to be created.
        :param pulumi.Input[_builtins.bool] replace: Replace (`true`) or merge (`false`) the permissions with existing permissions. When `true`, all existing permissions for the principal on this token will be replaced with the specified permissions. When `false`, the specified permissions will be merged with existing permissions. Default: `true`.
        :param pulumi.Input[_builtins.str] token: The security token for the resource. Use the `get_security_namespace_token` data source to generate tokens for specific resources. Changing this forces a new resource to be created.
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _SecurityPermissionsState.__new__(_SecurityPermissionsState)

        __props__.__dict__["namespace_id"] = namespace_id
        __props__.__dict__["permissions"] = permissions
        __props__.__dict__["principal"] = principal
        __props__.__dict__["replace"] = replace
        __props__.__dict__["token"] = token
        return SecurityPermissions(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter(name="namespaceId")
    def namespace_id(self) -> pulumi.Output[_builtins.str]:
        """
        The ID of the security namespace. Use the `get_security_namespaces` data source to discover available namespaces. Changing this forces a new resource to be created.
        """
        return pulumi.get(self, "namespace_id")

    @_builtins.property
    @pulumi.getter
    def permissions(self) -> pulumi.Output[Mapping[str, _builtins.str]]:
        """
        A map of permission names to permission values. All permission names specified must be valid for the given namespace, or an error will be returned. Permission values must be one of:
        - `Allow` (or `allow`, `ALLOW`) - Grant the permission
        - `Deny` (or `deny`, `DENY`) - Explicitly deny the permission
        - `NotSet` (or `notset`, `NOTSET`) - Remove the permission (inherit from parent)
        """
        return pulumi.get(self, "permissions")

    @_builtins.property
    @pulumi.getter
    def principal(self) -> pulumi.Output[_builtins.str]:
        """
        The descriptor or identity ID of the principal (user or group). Changing this forces a new resource to be created.
        """
        return pulumi.get(self, "principal")

    @_builtins.property
    @pulumi.getter
    def replace(self) -> pulumi.Output[Optional[_builtins.bool]]:
        """
        Replace (`true`) or merge (`false`) the permissions with existing permissions. When `true`, all existing permissions for the principal on this token will be replaced with the specified permissions. When `false`, the specified permissions will be merged with existing permissions. Default: `true`.
        """
        return pulumi.get(self, "replace")

    @_builtins.property
    @pulumi.getter
    def token(self) -> pulumi.Output[_builtins.str]:
        """
        The security token for the resource. Use the `get_security_namespace_token` data source to generate tokens for specific resources. Changing this forces a new resource to be created.
        """
        return pulumi.get(self, "token")

